"'use strict';\nconst fs = require('fs');\nconst path = require('path');\nconst filePath = path.resolve('D:/taffy-fan-site/frontend/src/App.tsx');\nlet code = fs.readFileSync(filePath, 'utf8');\n\n// 1. 添加 import ReviewPosts\nif (!code.includes('ReviewPosts')) {\n    code = code.replace(\n        \"import AdminDashboard from './pages/Admin/Dashboard';\",\n        \"import AdminDashboard from './pages/Admin/Dashboard';\\nimport ReviewPosts from './pages/Admin/ReviewPosts';\"\n    );\n    console.log('Added import');\n}\n\n// 2. 添加审核路由，放在 /admin 路由之后\nif (!code.includes('/admin/review')) {\n    code = code.replace(\n        \"<Route path=\\\\"admin\\\" element={\",\n        \"<Route path=\\\\"admin/review\\\" element={\\n                        token ? <ReviewPosts /> : <Navigate to=\\\"/login\\\" />\\n                    } />\\n                    <Route path=\\\"admin\\\" element={\"\n    );\n    console.log('Added route');\n}\n\nfs.writeFileSync(filePath, code, 'utf8');\nconsole.log('Done');"